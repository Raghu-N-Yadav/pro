<h1>Hello, Rails!</h1>

<h3>This is Article data</h3>

<ul>
  <% @articles.each do |article|%>
    <li>
      <%= link_to article.title, article%>
    </li>
  <% end %>
</ul>

<%= link_to "New Article", new_page_path %>